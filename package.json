{
  "name": "my-todo-app",
  "version": "1.0.0",
  "description": "A sophisticated to-do list application with modern UI",
  "main": "script.js",
  "scripts": {
    "build": "echo '🚀 Building the application...' && (if not exist dist mkdir dist) && copy index.html dist\\ && copy style.css dist\\ && copy script.js dist\\ && echo '✅ Build completed!'",
    "test": "echo '🧪 Running tests...' && node test.js",
    "lint": "echo '🔍 Running code quality checks...' && npx eslint script.js --no-warn-ignored || echo 'Code quality check completed with warnings'"
  },
  "keywords": ["todo", "jenkins", "pipeline", "web-app"],
  "author": "HashiniDeakin",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^8.0.0"
  }
}
